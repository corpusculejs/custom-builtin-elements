language: node_js
node_js: lts/*
matrix:
  include:
    - os: osx
      osx_image: xcode11
      addons:
        chrome: stable
branches:
  only:
    - master
script:
  - npm run test:coverage
after_success:
  - ./node_modules/.bin/codecov -e TRAVIS_NODE_VERSION -f .coverage/lcov.info
